{
  "name": "flow-envision",
  "version": "1.0.0",
  "description": "Envision &lt;> Flow Web Application",
  "main": "index.js",
  "scripts": {
    "start": "node server.js",
    "dev": "npm run build && npm run start",
    "build": "echo 'Build script to be configured'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "firebase": "^12.6.0"
  }
}
